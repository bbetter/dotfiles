# hyprlock — themed by ~/.config/scripts/gen-theme.sh from the wal palette.
# Edit this template, not hyprlock.conf.
#
# Activation (once): sudo pacman -S hyprlock hypridle
#   binds.lua      : SUPER+SHIFT+ESCAPE -> "loginctl lock-session"
#   autostart.lua  : launch "hypridle" instead of idle.sh
# idle.sh / swayidle / swaylock can stay installed as a fallback.

$font = JetBrainsMono Nerd Font

background {
    monitor =
    path = screenshot          # blur the live desktop (no wallpaper file needed)
    blur_passes = 3
    blur_size = 5
    brightness = 0.70
    contrast = 0.90
    vibrancy = 0.15
}

input-field {
    monitor =
    size = 320, 56
    position = 0, -90
    halign = center
    valign = center

    outline_thickness = 2
    rounding = 14
    dots_size = 0.28
    dots_spacing = 0.30
    dots_center = true

    outer_color = rgba(__PRIMARY_HEX__ff)
    inner_color = rgba(__BG__d9)
    font_color  = rgba(__FG__ff)
    check_color = rgba(__SUCCESS_HEX__ff)
    fail_color  = rgba(__ERROR_HEX__ff)

    placeholder_text = <span foreground="##__FG__">󰌾  Password</span>
    fail_text = <span foreground="##__ERROR_HEX__">󰀦  $FAIL (attempt $ATTEMPTS)</span>
    fade_on_empty = false
}

# Clock
label {
    monitor =
    text = cmd[update:1000] date +"%H:%M"
    font_family = $font
    font_size = 92
    color = rgba(__FG__ff)
    position = 0, 170
    halign = center
    valign = center
}

# Date
label {
    monitor =
    text = cmd[update:60000] date +"%A, %d %B"
    font_family = $font
    font_size = 20
    color = rgba(__FG__cc)
    position = 0, 80
    halign = center
    valign = center
}

# User
label {
    monitor =
    text =    $USER
    font_family = $font
    font_size = 15
    color = rgba(__PRIMARY_HEX__ff)
    position = 0, -28
    halign = center
    valign = center
}
